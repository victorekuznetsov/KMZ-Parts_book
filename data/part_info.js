/* Расширенные сведения о детали для карточки: фото, характеристики,
   цепочка замен номера. Необязательный файл — карточка работает и без него
   (наименования и цены берутся из прайса, «где применяется» считается по
   каталогам). Формат:
     window.PART_INFO = {
       "<номер детали>": {
         name:  "наименование",
         img:   ["url или путь к фото", ...],
         attrs: [["Масса, кг", "12.4"], ["Габариты, мм", "120×80×40"]],
         prev:  ["номер, который заменён этой деталью", ...],
         next:  ["номер, на который заменена эта деталь", ...]
       }
     };
   Генерируется скриптом build/gen_part_info.js. */
window.PART_INFO = {};
